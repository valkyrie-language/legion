[workspace]
members = ["projects/*"]
default-members = [
    "projects/legion-app",
    "projects/legion-config",
]
exclude = [
    "projects/.DS_Store",
]

[profile.release]
lto = true
panic = "abort"
